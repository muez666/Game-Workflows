name: Gtxgaming Auto Renew

on:
  schedule:
    # 每天中午12点运行（北京时间 UTC+8，对应 UTC 时间 04:00）
    - cron: '0 4 * * *'
  
  workflow_dispatch:  # 允许手动触发

jobs:
  renew:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout private script repository
      uses: actions/checkout@v4
      with:
        repository: ${{ github.repository_owner }}/Game-Auto-Scripts  # 格式: username/repo-name
        token: ${{ secrets.PRIVATE_REPO_TOKEN }}  # 需要有读取私库权限的 PAT
        path: scripts
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Check dependencies
      run: |
        curl --version
        python --version
    
    - name: Run renew script
      env:
        PANEL_DOMAIN: ${{ secrets.PANEL_DOMAIN }}
        API_KEY: ${{ secrets.API_KEY }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        TZ: Asia/Shanghai
      working-directory: scripts/Gtxgaming
      run: python main.py
    
    - name: Upload JSON artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: renew-data-${{ github.run_number }}
        path: |
          scripts/Gtxgaming/servers_list.json
          scripts/Gtxgaming/servers_info.json
          scripts/Gtxgaming/renew_response_*.json
        retention-days: 30
        if-no-files-found: ignore
